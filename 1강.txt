sql(structured Query Language): 데이터베이스와 소통하는 언어
database마다 sql이 조금씩 다르다.
mysql, oracle, postgreSQL의 sql이 조금씩 달라서 실행이 안되는 경우도 있다.
그래서 sql표준인 ansi sql을 정해서 모든 데이터베이스에서 사용될 수 있는 언어를 정했다.

database cheatsheet에서 사용되는 sql과 설명을 볼 수 있다.